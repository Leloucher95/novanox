# Tests

- `unit/` : calculs, validation, permissions et transitions de statut ;
- `integration/` : adaptateurs et règles avec Firebase Emulator Suite ;
- `e2e/` : parcours critiques avec Playwright.

Aucun assouplissement des règles Firestore ou Storage ne doit être accepté sans test d’émulateur correspondant.
